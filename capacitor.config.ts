import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appMantenimientoS.app',
  appName: 'Reportes',
  webDir: 'dist/myapp/browser'
};

export default config;
